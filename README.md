# Smell test

```
Usage: smell-test [options] <log-line> <command...>

Runs a cli command and waits on a specific log line to appear

Options:
  -V, --version  output the version number
  -h, --help     display help for command
```